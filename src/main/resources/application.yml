micronaut:
  application:
    name: mealShoppingCompanion
datasources:
  default:
    url: jdbc:postgresql://localhost:5433/smart_fridge
    username: postgres
    password: '314159'
    driver-class-name: org.postgresql.Driver
    db-type: postgres
    schema-generate: CREATE_DROP
    dialect: POSTGRES
netty:
  default:
    allocator:
      max-order: 3
